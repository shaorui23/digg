<%= form_for :redisset, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Redis Set</legend>

    <div class="control-group">
      <%= f.label "Type", :class => 'control-label' %>
      <div class="controls">
        <select class="span2">
          <option>String</option>
          <option>Set</option>
          <option>List</option>
        </select>
      </div>
    </div>

    <div class="control-group">
      <%= f.label "Key", :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label "Value", :class => 'control-label' %>
      <div class="controls">
        <%= f.text_area :redis_value, :class => 'text_area', :placeholder => "以逗号分割" %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', records_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
