<%= form_for @objected, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Objected</legend>

    <div class="control-group">
      <%= f.label :prefix_list_for_redis, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :prefix_list_for_redis, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', objecteds_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
