
<ul class="nav nav-tabs">
  <li class="active"><a href="#home" data-toggle="tab"><i class="icon-home"></i>Redis Info</a></li>
  <li><a href="#profile" data-toggle="tab"><i class="icon-picture"></i>Operation Graph</a></li>
  <li><a href="#messages" data-toggle="tab"><i class="icon-pencil"></i>Commandly-Used Commands</a></li>
  <li><a href="#settings" data-toggle="tab"><i class="icon-edit"></i>Settings</a></li>
</ul>
<div class="tab-content">
  <div class="tab-pane active" id="home">
    <table class="table table-condensed table-striped">
      <thead>
        <tr>
          <th><h2></h2></th>
        </tr>
      </thead>
      <tbody>
        <% (0..@info.size).step(2) do |a| %>
          <tr>
            <% @info.to_a.slice(a, 2).each do |v| %>
              <td><strong><%= v[0].titleize %></strong></td>
              <td><em><%= v[1] %></em></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="tab-pane" id="profile">
    <div class='row'>
      <div class='span15'>
        <div class='row'>
          <div class='span15'>
            <div class='widget'>
              <div class='headbar'>
                <h3>Redis Type DataUse</h3>
              </div>
              <div id='redis_use' style='width:880px;height:325px'>></div>
              <div class='headbar'>
                <h3>Memory Usage</h3>
              </div>
              <div id='memory-container' style='width:880px;height:225px'></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="tab-pane" id="messages">
      <h3>Flush DataBase</h3>
      <p>It will excute the command that flush all the keys in the local server. View <a href="http://redis.readthedocs.org/en/latest/server/flushdb.html" target= "blank" rel="tooltip" title="click me and go to Redis official website: redis.readthedocs.org">the Redis docs</a> for that.</p>
      <pre>redis> DBSIZE   # 清空前的 key 数量
(integer) 4
redis> FLUSHDB 
<strong>OK</strong> 
redis> DBSIZE   # 清空后的 key 数量 
(integer) 0 
      </pre>
      <div class="btns">
        <button class="btn btn-danger" id="flush-btn">Flush DataBase</button>
      </div>
      <br />
      <h3>Reset DataBase</h3>
      <p><pre>CONFIG RESETSTAT 重置 INFO 命令中的某些统计数据，包括：
Keyspace hits (键空间命中次数)
Keyspace misses (键空间不命中次数)
Number of commands processed (执行命令的次数)
Number of connections received (连接服务器的次数)
Number of expired keys (过期key的数量)
Number of rejected connections (被拒绝的连接数量)
Latest fork(2) time(最后执行 fork(2) 的时间)
The aof_delayed_fsync counter(aof_delayed_fsync 计数器的值)</pre>
     </p>
      <button class="btn btn-danger" id="reset-btn">Reset DataBase</button>
  </div>
  <div class="tab-pane" id="settings">
    <form class="form-horizontal">
      <fieldset>
        <legend>Legend text</legend>
        <div class="control-group">
          <label class="control-label" for="input01">Text input</label>
          <div class="controls">
            <input type="text" class="input-xlarge" id="input01">
            <p class="help-block">Supporting help text</p>
          </div>
        </div>
      </fieldset>
    </form>
    <a href="#" class="label label-info" rel="tooltip" title="first tooltip">hover over me</a>
    <form class="well">
      <label>Label name</label>
      <input type="text" class="span3" placeholder="Type something…">
      <span class="help-block">Example block-level help text here.</span>
      <label class="checkbox">
        <input type="checkbox"> Check me out
      </label>
      <button type="submit" class="btn">Submit</button>
    </form>
     <table class="table table-bordered table-striped">
        <tbody>
          <tr>
             <td>Single toggle</td>
             <td>
                <button class="btn btn-primary" data-toggle="button">Single Toggle</button>
              </td>
           </tr>
          <tr>
             <td>Radio</td>
             <td>
                <div class="btn-group" data-toggle="buttons-radio">
                  <button class="btn btn-primary">On</button>
                  <button class="btn btn-primary">OFF</button>
                </div>
             </td>
           </tr>
        </tbody>
     </table>
  </div>
</div>

